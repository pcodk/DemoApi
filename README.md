Master class demo API
=====================

This is a very simple GoLang base API. It simply exposes a api server running on port 8080 (configurable with --port

It is only used for internal demo puposes.